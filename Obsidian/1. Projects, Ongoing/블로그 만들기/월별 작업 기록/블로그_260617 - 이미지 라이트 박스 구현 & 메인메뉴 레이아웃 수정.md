
>[!done]
>1. 이미지 라이트 박스(글에서 이미지 클릭 시 확대 / 축소할 수 있는 컴포넌트) 추가
>2. 메인메뉴 레이아웃 수정 : `Recent`에서도 `Subsection` 표기


- `ImageLightBox` : 게시글에 있는 이미지를 클릭했을 때 확대 / 축소할 수 있는 기능
- 대부분의 기능 구현은 AI에게 맡겼다. 난 연결하고 수정하는 역할만 함

## 이미지 라이트박스
### ImageLightBox.tsx - UI 컴포넌트
```tsx
import React, { useState, useEffect, useCallback, useRef } from 'react';
import ReactDOM from 'react-dom';

import { IoClose } from 'react-icons/io5';
import { FiZoomIn, FiZoomOut } from 'react-icons/fi';
import { MdOutlineZoomOutMap } from 'react-icons/md';

interface ImageLightboxProps {
  src: string;
  alt?: string;
  onClose: () => void;
}

const isMobile = () => window.matchMedia('(pointer: coarse)').matches;

const MIN_SCALE = 0.5; // 최소 배율 (최대한 작게 보임)
const MAX_SCALE = 3; // 최대 배율 (세세한 부분까지 보임)
const SCALE_STEP = 0.25;

// 이미지 축소 / 확대를 위한 컴포넌트. 
const ImageLightbox: React.FC<ImageLightboxProps> = ({ src, alt = '이미지', onClose }) => {
  const [scale, setScale] = useState(1);
  const [translate, setTranslate] = useState({ x: 0, y: 0 });

  // 드래그 상태
  const isDraggingRef = useRef(false);
  const lastPointerRef = useRef({ x: 0, y: 0 });
  const overlayRef = useRef<HTMLDivElement>(null); // 오버레이 DOM 요소 참조를 위한 ref

  // 핀치-줌 상태 (모바일)
  const lastPinchDistRef = useRef<number | null>(null);

  // ── 유틸 ──────────────────────────────────────────────
  const clampScale = (s: number) => Math.min(MAX_SCALE, Math.max(MIN_SCALE, s));

  const resetView = useCallback(() => {
    setScale(1);
    setTranslate({ x: 0, y: 0 });
  }, []);

  // ── 키보드 ────────────────────────────────────────────
  useEffect(() => {
    const onKey = (e: KeyboardEvent) => {
      if (e.key === 'Escape') onClose();
      if (e.key === '+' || e.key === '=') setScale(s => clampScale(s + SCALE_STEP));
      if (e.key === '-') setScale(s => clampScale(s - SCALE_STEP));
      if (e.key === '0') resetView();
    };
    window.addEventListener('keydown', onKey);
    return () => window.removeEventListener('keydown', onKey);
  }, [onClose, resetView]);

  // ── 스크롤 방지 ───────────────────────────────────────
  useEffect(() => {
    const prev = document.body.style.overflow;
    document.body.style.overflow = 'hidden';
    return () => { document.body.style.overflow = prev; };
  }, []);

  // ── 마우스 휠 줌 ─────────────────────────────────────
  useEffect(() => {
    const el = overlayRef.current;
    if (!el) return;
    const onWheel = (e: WheelEvent) => {
        e.preventDefault();
        e.stopPropagation();
        
        // 아래 스크롤 시 이미지가 작아짐
        const delta = e.deltaY < 0 ? SCALE_STEP : -SCALE_STEP;

        setScale(s => clampScale(s + delta));
    };
    
    // 기본 설정인 passive: true으로 등록 시 preventDefault()가 무시된다.
    // 네이티브 이벤트로 등록하면서 passive: false를 명시해서 preventDefault()와 stopPropagation()이 실제로 동작하게 함
    el.addEventListener('wheel', onWheel, { passive: false });

    return () => el.removeEventListener('wheel', onWheel);
  })


// 이벤트 전파가 뚫리는 경우가 있어서 상단의 useEffect()로 수정
//   const handleWheel = useCallback((e: React.WheelEvent) => {
//     e.preventDefault();
//     const delta = e.deltaY < 0 ? SCALE_STEP : -SCALE_STEP;
//     setScale(s => clampScale(s + delta));
//   }, []);

  // ── 마우스 드래그 ─────────────────────────────────────
  const handleMouseDown = useCallback((e: React.MouseEvent) => {
    if (scale <= 1) return; // 원본 크기일 땐 드래그 비활성
    isDraggingRef.current = true;
    lastPointerRef.current = { x: e.clientX, y: e.clientY };
    e.preventDefault();
  }, [scale]);

  const handleMouseMove = useCallback((e: React.MouseEvent) => {
    if (!isDraggingRef.current) return;
    const dx = e.clientX - lastPointerRef.current.x;
    const dy = e.clientY - lastPointerRef.current.y;
    lastPointerRef.current = { x: e.clientX, y: e.clientY };
    setTranslate(t => ({ x: t.x + dx, y: t.y + dy }));
  }, []);

  const stopDrag = useCallback(() => { isDraggingRef.current = false; }, []);

  // ── 터치 (드래그 + 핀치줌) ───────────────────────────
  const handleTouchStart = useCallback((e: React.TouchEvent) => {
    if (e.touches.length === 2) {
      const dx = e.touches[0].clientX - e.touches[1].clientX;
      const dy = e.touches[0].clientY - e.touches[1].clientY;
      lastPinchDistRef.current = Math.hypot(dx, dy);
    } else if (e.touches.length === 1) {
      lastPointerRef.current = { x: e.touches[0].clientX, y: e.touches[0].clientY };
      isDraggingRef.current = true;
    }
  }, []);

  const handleTouchMove = useCallback((e: React.TouchEvent) => {
    e.preventDefault();
    if (e.touches.length === 2 && lastPinchDistRef.current !== null) {
      const dx = e.touches[0].clientX - e.touches[1].clientX;
      const dy = e.touches[0].clientY - e.touches[1].clientY;
      const dist = Math.hypot(dx, dy);
      const ratio = dist / lastPinchDistRef.current;
      lastPinchDistRef.current = dist;
      setScale(s => clampScale(s * ratio));
    } else if (e.touches.length === 1 && isDraggingRef.current && scale > 1) {
      const dx = e.touches[0].clientX - lastPointerRef.current.x;
      const dy = e.touches[0].clientY - lastPointerRef.current.y;
      lastPointerRef.current = { x: e.touches[0].clientX, y: e.touches[0].clientY };
      setTranslate(t => ({ x: t.x + dx, y: t.y + dy }));
    }
  }, [scale]);

  const handleTouchEnd = useCallback(() => {
    isDraggingRef.current = false;
    lastPinchDistRef.current = null;
  }, []);

  // ── 스케일 리셋 (1배 이하로 내리면 translate도 초기화) ──
  useEffect(() => {
    if (scale <= 1) setTranslate({ x: 0, y: 0 });
  }, [scale]);

  // ── 렌더 ─────────────────────────────────────────────
const portal = (
    <div
      ref = {overlayRef}
      className="fixed inset-0 z-[9999] flex items-center justify-center bg-black/85 backdrop-blur-sm"
      onClick={onClose}
      style={{ touchAction: 'none' }}
    >
        
      {/* 컨트롤 버튼 (우측 상단) — 데스크탑 전용 */}
      <div
        className="absolute top-3 right-3 flex items-center gap-2 z-10"
        onClick={e => e.stopPropagation()}
      >
        {!isMobile() && (
          <>
            {/* 여기 div들은 원래 Button이었으나 flex items-center justify-center와 충돌해서 아이콘이 사라지는 이슈가 있었다.
            button 태그들을 div로 수정하니까 동작하므로 이렇게 냅둠. */}
            <div
              role = "button"
              onClick={() => setScale(s => clampScale(s - SCALE_STEP))}
              className="w-9 h-9 flex items-center justify-center rounded-full  bg-white/15 hover:bg-white/30 text-white transition-colors"
              aria-label="축소"
            >
              <FiZoomOut size={18}/>
            </div>
 
            <span className="text-white/80 text-xs font-mono w-10 text-center select-none">
              {Math.round(scale * 100)}%
            </span>
 
            <div
              role = "button"
              onClick={() => setScale(s => clampScale(s + SCALE_STEP))}
              className="w-9 h-9 flex items-center justify-center rounded-full bg-white/15 hover:bg-white/30 text-white transition-colors"
            //   className="w-9 h-9 flex items-center justify-center rounded-full text-white transition-colors"
              aria-label="확대"
            >
              <FiZoomIn size={18}/>
            </div>
 
            <div
              role = "button"
              onClick={resetView}
              className="w-9 h-9 flex items-center justify-center rounded-full bg-white/15 hover:bg-white/30 text-white transition-colors"
            //   className="w-9 h-9 flex items-center justify-center rounded-full text-white transition-colors"
              aria-label="원래 크기"
            >
              <MdOutlineZoomOutMap size={18}/>
            </div>
          </>
        )}
 
        {/* 닫기 버튼은 모바일/데스크탑 공통 */}
        <div
          role = "button"
          onClick={onClose}
          className="w-9 h-9 flex items-center justify-center rounded-full bg-white/15 hover:bg-white/30 text-white transition-colors ml-1"
        //   className="w-9 h-9 flex items-center justify-center rounded-full  text-white transition-colors ml-1"

          aria-label="닫기"
        >
          <IoClose size={20}/>
        </div>
      </div>
 
      {/* 이미지 래퍼 */}
      <div
        className="relative max-w-[90vw] max-h-[90vh] flex items-center justify-center"
        onClick={e => e.stopPropagation()}
        // onWheel={handleWheel}
        onMouseDown={handleMouseDown}
        onMouseMove={handleMouseMove}
        onMouseUp={stopDrag}
        onMouseLeave={stopDrag}
        onTouchStart={handleTouchStart}
        onTouchMove={handleTouchMove}
        onTouchEnd={handleTouchEnd}
        style={{
          cursor: scale > 1 ? (isDraggingRef.current ? 'grabbing' : 'grab') : 'default',
          userSelect: 'none',
        }}
      >
        <img
          src={src}
          alt={alt}
          draggable={false}
          style={{
            maxWidth: '90vw',
            maxHeight: '90vh',
            objectFit: 'contain',
            transform: `scale(${scale}) translate(${translate.x / scale}px, ${translate.y / scale}px)`,
            transition: isDraggingRef.current ? 'none' : 'transform 0.15s ease',
            borderRadius: '4px',
            boxShadow: '0 8px 40px rgba(0,0,0,0.6)',
          }}
        />
      </div>
 
      {/* 하단 힌트 — 데스크탑 전용 */}
      {!isMobile() && (
        <p className="absolute bottom-4 left-0 right-0 text-center text-white/40 text-xs select-none pointer-events-none">
          스크롤로 확대/축소{scale > 1 ? ' · 드래그로 이동' : ''} · ESC로 닫기
        </p>
      )}
    </div>
  );

  return ReactDOM.createPortal(portal, document.getElementById('modal-root')!);
};

export default ImageLightbox;
```

- 설명(AI 요약)
	- 이미지를 클릭했을 때 화면 위에 올라오는 어두운 오버레이와 이미지를 렌더링합니다. 
	- `modal-root` Portal에 마운트되므로 기존 모달(ReviewModal) 위에도 자연스럽게 올라옵니다. 
	- 데스크탑에서는 마우스 휠 줌, 드래그 이동, 컨트롤 버튼(확대/축소/리셋/닫기)을 제공하고, 모바일에서는 핀치 줌과 터치 드래그만 제공합니다. 배경 클릭, ESC 키, 닫기 버튼으로 닫을 수 있습니다.

### useImageLightBox.ts
```ts
import { useState, useEffect, useCallback } from 'react';

interface LightboxState {
  src: string;
  alt: string;
}

export function useImageLightbox(containerSelector: string) {
  const [lightbox, setLightbox] = useState<LightboxState | null>(null);

  const openLightbox = useCallback((src: string, alt: string) => {
    setLightbox({ src, alt });
  }, []);

  const closeLightbox = useCallback(() => {
    setLightbox(null);
  }, []);

  useEffect(() => {
    const handler = (e: MouseEvent) => {
      const target = e.target as HTMLElement;
      if (target.tagName !== 'IMG') return;
      if (target.closest('.ql-toolbar') || target.closest('.ql-resizer')) return;

      const img = target as HTMLImageElement;
      openLightbox(img.src, img.alt || '이미지');
    };

    // 등록된 컨테이너를 추적 (클린업에서 제거하기 위해)
    const attachedContainers: HTMLElement[] = [];

    const attach = () => {
      document
        .querySelectorAll<HTMLElement>(containerSelector)
        .forEach(c => {
          c.addEventListener('click', handler);
          attachedContainers.push(c);
        });
    };

    attach();

    // 아직 DOM에 없으면 MutationObserver로 대기
    let observer: MutationObserver | null = null;
    if (attachedContainers.length === 0) {
      observer = new MutationObserver(() => {
        const found = document.querySelectorAll<HTMLElement>(containerSelector);
        if (found.length > 0) {
          found.forEach(c => {
            c.addEventListener('click', handler);
            attachedContainers.push(c);
          });
          observer?.disconnect();
          observer = null;
        }
      });
      observer.observe(document.body, { childList: true, subtree: true });
    }

    return () => {
      attachedContainers.forEach(c => c.removeEventListener('click', handler));
      observer?.disconnect();
    };
  }, [containerSelector, openLightbox]);

  return { lightbox, closeLightbox };
}
```
- 설명(AI 요약)
	- CSS 선택자를 받아서 해당 영역 안의 이미지 클릭을 감지하고 라이트박스 열림/닫힘 상태를 관리합니다. 
	- 이벤트 위임 방식으로 동작하며, 비동기 렌더링으로 DOM이 늦게 생기는 경우를 대비해 `MutationObserver`로 컨테이너가 나타날 때까지 대기합니다.

### 기존 컴포넌트에 적용
- `ReviewModal`
```tsx
import ImageLightbox from '../common/ImageLightbox';
import { useImageLightbox } from '../../hooks/useImageLightbox';

  const { lightbox, closeLightbox } = useImageLightbox('.review-content img');
  
  return ReactDOM.createPortal(
    <>
      {modalContent}, 
      {lightbox && (
        <ImageLightbox 
          src = {lightbox.src}
          alt = {lightbox.alt}
          onClose = {closeLightbox}
        />
      )}
    </>,
    document.getElementById('modal-root')!
  );  
```

- `PostDetail`
```tsx
import ImageLightbox from '../components/common/ImageLightbox';
import { useImageLightbox } from '../hooks/useImageLightbox';

    const { lightbox, closeLightbox } = useImageLightbox('.ql-editor img');
    
	return (
		// ...
		
		// 최하단
		{lightbox && (
			<ImageLightbox
			src={lightbox.src}
			alt={lightbox.alt}
			onClose={closeLightbox}
			/>
		)}
)
```

### 스샷
- PC 환경 `ImageLightbox`
![[Pasted image 20260617154707.png]]

- 모바일 환경 `ImageLightBox`
![[Pasted image 20260617160507.png]]
> 밖에 하얀색 보이는 건 실제로는 그렇지 않음


### 진행 중 수정한 사항
#### 1. 모바일 환경에서 확대 / 축소 / 원본 크기로 보는 버튼 및 하단 힌트 박스 보이지 않게 변경

#### 2. 버튼들의 이미지 아이콘 보이지 않는 문제 수정
- `button`에 `flex items-center justify-center`을 함께 쓰면 중심에 들어가야 할 아이콘이 어디론가 벗어나 있다. 
- **`div`로 태그 변경**하면 잘 됨.
	- 버튼의 배경을 없애는 편이 깔끔하긴 한데, 배경 색이 없을 경우 이미지에 버튼이 묻혀서 보이지 않는 현상이 있다. 반투명 배경은 안전망 역할을 한다.

#### 3. `ImageLightbox`가 나타난 상태에서 마우스 스크롤이 외부까지 영향을 미치는 현상 수정
```tsx
{/* 이미지 래퍼 */}
<div
	className="relative max-w-[90vw] max-h-[90vh] flex items-center justify-center"
	onClick={e => e.stopPropagation()}
	onWheel={handleWheel}

// handleWheel 관련
const handleWheel = useCallback((e: React.WheelEvent) => {
e.preventDefault();
const delta = e.deltaY < 0 ? SCALE_STEP : -SCALE_STEP;
setScale(s => clampScale(s + delta));
}, []);
```
- `handleWheel`에서 `e.preventDefault()`을 사용하고 있음 
- `React`의 합성 이벤트는 `passive Listener`로 등록, `preventDefault()`가 무시되는 브라우저가 있다. 이 때만 전파가 뚫린다.
	- (스크롤이 외부까지 영향을 미치는 현상이 항상 동작하는 건 아니었음)

- 아래처럼 수정
```tsx
const overlayRef = useRef<HTMLDivElement>(null); // 오버레이 DOM 요소 참조를 위한 ref

// ── 마우스 휠 줌 ─────────────────────────────────────
useEffect(() => {
	const el = overlayRef.current;
	if (!el) return;
	const onWheel = (e: WheelEvent) => {
		e.preventDefault();
		e.stopPropagation();
		
		// 아래 스크롤 시 이미지가 작아짐
		const delta = e.deltaY < 0 ? SCALE_STEP : -SCALE_STEP;
		
		setScale(s => clampScale(s + delta));
	};
	
	// 기본 설정인 passive: true으로 등록 시 preventDefault()가 무시된다.
	// 네이티브 이벤트로 등록하면서 passive: false를 명시해서 preventDefault()와 stopPropagation()이 실제로 동작하게 함
	el.addEventListener('wheel', onWheel, { passive: false });
	
	return () => el.removeEventListener('wheel', onWheel);
})


// 이벤트 전파가 뚫리는 경우가 있어서 상단의 useEffect()로 수정
//   const handleWheel = useCallback((e: React.WheelEvent) => {
//     e.preventDefault();
//     const delta = e.deltaY < 0 ? SCALE_STEP : -SCALE_STEP;
//     setScale(s => clampScale(s + delta));
//   }, []);

const portal = (
    <div
      ref = {overlayRef}
    // ...
```
1. `overlayRef`는 이 컴포넌트 전체의 참조라고 봐도 무방
2. `useEffect()`문
	1. 기본 : 컴포넌트가 마운트될 때 `onWheel`이라는 이벤트 리스너를 붙이고 언마운트 될 때 제거
	2. `onWheel` 문 : 휠이 움직였을 때의 실제 동작

자세한 내용은 주석으로 다 표기해둠

#### 4. 확대 배율 설정
- 최초에 받은 코드는 0.5 ~ 5, 간격 0.3
- 수정 : 0.5 ~ 3, 간격 0.25

#### 5. 힌트 박스 표시 기능
```tsx
{!isMobile() && (
<p className="absolute bottom-4 left-0 right-0 text-center text-white/40 text-xs select-none pointer-events-none">
  스크롤로 확대/축소{scale > 1 ? ' · 드래그로 이동' : ''} · ESC로 닫기
</p>
)}
```
- 드래그는 원본 배율 이상일 때만 적용되는 설정이므로, 그 때만 텍스트가 나타노도록 변경

## 메인메뉴 레이아웃 수정
![[Pasted image 20260617165008.png]]
- '최근 게시글'의 탭인데, 특정 글은 `Subsection`이 없으면 어떤 것에 대해 글을 작성한 것인지 알 수 없음
- 그래서 `Subsection`에 대한 부분을 추가해줬다.
```tsx
// 기존 구조
<div className="flex items-center justify-between mb-1">
  {/* 섹션 */}
  <Link
	to={`/${category}/${post.section}/page/1`}
	className="px-2 py-1 text-xs font-mono font-semibold bg-gray-200 text-gray-700 rounded"
>
	{post.section}
  </Link>

  {/* 서브섹션 */}
  <Link
	to={`/${category}/${post.section}/subsection/${post.subsection}/page/1`}
	className="px-1 py-0.5 text-xs font-mono font-semibold bg-gray-200 text-gray-700 rounded"
	style={{
		backgroundColor: subsectionStyle[post.subsection]?.backgroundColor,
		color: subsectionStyle[post.subsection]?.textColor
	}}
>
	{post.subsection}
  </Link>

  {/* 글 생성일 */}
  <span className="text-xs text-gray-500">{post.created_at}</span>
</div>
```

```tsx
// 수정된 구조
<li key={post.slug} className="px-4 py-3 hover:bg-gray-100">
<div className = "flex items-center gap-1 mb-1">
  {/* 섹션 */}
  <Link
	to={`/${category}/${post.section}/page/1`}
	className="px-2 py-1 text-xs font-mono font-bold bg-gray-200 text-gray-700 hover:text-gray-700 rounded"
>
	{post.section}
  </Link>

  {/* 서브섹션 */}
  <Link
	to={`/${category}/${post.section}/subsection/${post.subsection}/page/1`}
	className="px-2 py-1 text-xs font-mono bg-gray-200 text-gray-700 hover:text-gray-700 rounded"
	style={{
		backgroundColor: subsectionStyle[post.subsection]?.backgroundColor,
		color: subsectionStyle[post.subsection]?.textColor
	}}
>
	{post.subsection}
  </Link>

  {/* 글 생성일 */}
  <span className="ml-auto text-xs text-gray-500">{post.created_at}</span>
</div>
<Link to={`/${category}/${post.section}/post/${post.slug}`} className="block">
  <h3 className="text-sm font-medium text-gray-900 hover:text-gray-700 truncate">
	{post.title}
  </h3>
</Link>
</li>
```

- 백엔드에서 `subsection`을 가져오는 구조가 아니었다면 이미지를 다시 만들어야 했는데, 그럴 필요는 없었다.

- 스타일도 프론트에서 한꺼번에 가져오는 구조라서 바로 처리할 수 있었음.
```tsx
const subsectionStyle = useSelector((state: RootState) => state.style);
```